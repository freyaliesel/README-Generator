{
  "name": "readme-generator",
  "version": "1.0.0",
  "description": "Assignment is to create a program that will generate a README file based on user input acquired via command-line prompts.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "inquirer": "^8.2.2"
  }
}
